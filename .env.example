# Environment Variables Example
# Copy this to .env.local and adjust values for your local development setup

# API Configuration
# Set USE_FLASK_BACKEND=true to use local Flask backend
# Set USE_FLASK_BACKEND=false to use Next.js mock APIs (recommended for development)
USE_FLASK_BACKEND=false

# Flask backend URL (only used when USE_FLASK_BACKEND=true)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5000

# Site URL for absolute URL generation
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Vercel URL (automatically set by Vercel deployment)
# VERCEL_URL=your-deployment.vercel.app

# Environment
NODE_ENV=development

# Production configuration example:
# USE_FLASK_BACKEND=false
# NEXT_PUBLIC_SITE_URL=https://xrpl-sale-frontend.vercel.app